{{ form_start(form) }}
    <label for="">Nom de la sous catégorie</label><br>
    {{ form_widget(form.name_category,{attr:{'class':'form form-control'}}) }}
    <br>
    <button class="btn btn-outline-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

