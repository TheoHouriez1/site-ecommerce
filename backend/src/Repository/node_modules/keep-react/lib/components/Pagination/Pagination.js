'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
import { PaginationContext } from './Context';
import { paginationTheme } from './theme';
const Pagination = forwardRef(({ className, children, shape = 'rounded', ...props }, ref) => {
    const { root } = paginationTheme;
    return (_jsx("div", { ...props, ref: ref, "aria-label": "pagination", className: cn(root.base, className), children: _jsx(PaginationContext.Provider, { value: { shape }, children: children }) }));
});
Pagination.displayName = 'Pagination';
export { Pagination };
