'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { cloneElement, forwardRef, isValidElement } from 'react';
import { cn } from '../../utils/cn';
import { paginationTheme } from './theme';
const PaginationNavigator = forwardRef(({ className, children, shape = 'rounded', asChild, ...props }, ref) => {
    const { navigator } = paginationTheme;
    if (asChild && isValidElement(children)) {
        return cloneElement(children, {
            itemRef: ref,
            ...props,
        });
    }
    return (_jsx("button", { ...props, ref: ref, className: cn(navigator.base, navigator.shape[shape], className), children: children }));
});
PaginationNavigator.displayName = 'PaginationNavigator';
export { PaginationNavigator };
