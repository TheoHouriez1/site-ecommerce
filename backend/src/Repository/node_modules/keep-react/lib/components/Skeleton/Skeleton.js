'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
import { skeletonTheme } from './theme';
const Skeleton = forwardRef(({ animation = true, children, className, ...props }, ref) => {
    const theme = skeletonTheme;
    return (_jsx("div", { ...props, ref: ref, className: cn(animation && theme.animation, theme.base, className), children: children }));
});
Skeleton.displayName = 'Skeleton';
export { Skeleton };
