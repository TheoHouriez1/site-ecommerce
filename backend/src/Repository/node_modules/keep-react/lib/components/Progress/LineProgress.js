'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef, useEffect, useState } from 'react';
import { cn } from '../../utils/cn';
import { ProgressContext } from './Context';
import { progressTheme } from './theme';
const LineProgress = forwardRef(({ progress = 0, children, className, ...props }, ref) => {
    const [progressBar, setProgressBar] = useState(0);
    const { line } = progressTheme;
    useEffect(() => {
        const timer = setTimeout(() => setProgressBar(progress), 200);
        return () => clearTimeout(timer);
    }, [progress]);
    return (_jsx("div", { ref: ref, ...props, className: cn(line.root, className), children: _jsx(ProgressContext.Provider, { value: { lineProgressBar: progressBar }, children: children }) }));
});
LineProgress.displayName = 'LineProgress';
export { LineProgress };
