'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { cloneElement, forwardRef, isValidElement } from 'react';
import { cn } from '../../utils/cn';
const CardTitle = forwardRef(({ children, asChild, className, ...props }, ref) => {
    if (asChild && isValidElement(children)) {
        return cloneElement(children, {
            itemRef: ref,
            ...props,
        });
    }
    return (_jsx("p", { ref: ref, className: cn('text-heading-6 font-semibold text-metal-800 dark:text-white', className), ...props, children: children }));
});
CardTitle.displayName = 'CardTitle';
export { CardTitle };
