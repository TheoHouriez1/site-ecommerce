'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
import { AlertContext } from './AlertContext';
import { alertTheme } from './theme';
const Alert = forwardRef(({ children, color = 'primary', withBg, dismiss, className, ...props }, ref) => {
    return (_jsx("div", { ...props, ref: ref, className: cn(alertTheme.root.base, withBg ? alertTheme.colorWithBg[color] : alertTheme.colorWithOutBg[color], dismiss ? 'hidden' : 'flex', className), children: _jsx(AlertContext.Provider, { value: { color }, children: children }) }));
});
Alert.displayName = 'Alert';
export { Alert };
