'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
import { useCarouselContext } from './CarouselContext';
import { DotButton, useDotButton } from './CarouselDotButton';
import { carouselTheme } from './theme';
export const CarouselIndicators = forwardRef(({ className, dotButtonStyle, ...props }, ref) => {
    const { emblaApi } = useCarouselContext();
    const theme = carouselTheme;
    const { selectedIndex, scrollSnaps, onDotButtonClick } = useDotButton(emblaApi);
    return (_jsx("div", { ...props, className: cn(theme.controls.indicators.container, className), ref: ref, children: scrollSnaps.map((number, index) => (_jsx(DotButton, { "data-active": index === selectedIndex, onClick: () => onDotButtonClick(index), className: cn(theme.controls.indicators.dot, dotButtonStyle) }, number))) }));
});
CarouselIndicators.displayName = 'CarouselIndicators';
