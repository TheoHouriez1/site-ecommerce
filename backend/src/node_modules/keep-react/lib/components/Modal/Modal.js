'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { Root } from '@radix-ui/react-dialog';
import { ModalContext } from './Context';
const Modal = ({ children, showCloseIcon = true, ...props }) => {
    return (_jsx(Root, { ...props, children: _jsx(ModalContext.Provider, { value: { showCloseIcon }, children: children }) }));
};
export { Modal };
