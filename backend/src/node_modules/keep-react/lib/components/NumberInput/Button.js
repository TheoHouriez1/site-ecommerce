'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
export const NumberInputButton = forwardRef(({ className, children, ...props }, ref) => {
    return (_jsx("button", { ref: ref, ...props, className: cn('max-w-max flex-1', className), children: children }));
});
NumberInputButton.displayName = 'NumberInputButton';
