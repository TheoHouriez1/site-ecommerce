'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { cn } from '../../utils/cn';
import { useAlertContext } from './AlertContext';
import { alertTheme } from './theme';
const AlertDescription = forwardRef(({ children, className, ...props }, ref) => {
    const { description } = alertTheme;
    const { color = 'secondary' } = useAlertContext();
    return (_jsx("p", { ...props, className: cn(description.color[color], description.base, className), ref: ref, children: children }));
});
AlertDescription.displayName = 'AlertDescription';
export { AlertDescription };
