'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { cloneElement, forwardRef, isValidElement } from 'react';
import { cn } from '../../utils/cn';
const EmptyDescription = forwardRef(({ children, asChild, className, ...props }, ref) => {
    if (asChild && isValidElement(children)) {
        return cloneElement(children, {
            itemRef: ref,
            ...props,
        });
    }
    return (_jsx("p", { ref: ref, className: cn('text-center text-body-3 font-normal text-metal-400 dark:text-metal-300', className), ...props, children: children }));
});
EmptyDescription.displayName = 'EmptyDescription';
export { EmptyDescription };
