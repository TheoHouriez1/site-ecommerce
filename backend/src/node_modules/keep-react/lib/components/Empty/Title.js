'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { cloneElement, forwardRef, isValidElement } from 'react';
import { cn } from '../../utils/cn';
const EmptyTitle = forwardRef(({ children, asChild, className, ...props }, ref) => {
    if (asChild && isValidElement(children)) {
        return cloneElement(children, {
            itemRef: ref,
            ...props,
        });
    }
    return (_jsx("h1", { ref: ref, className: cn('text-heading-5 font-medium text-metal-700 dark:text-white', className), ...props, children: children }));
});
EmptyTitle.displayName = 'EmptyTitle';
export { EmptyTitle };
